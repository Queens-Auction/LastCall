spring:
  datasource:
    url: jdbc:mysql://localhost:3306/{DB이름}
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret:
    key:
  expiration: 86400000

logging:
  level:
    org.hibernate.SQL: debug